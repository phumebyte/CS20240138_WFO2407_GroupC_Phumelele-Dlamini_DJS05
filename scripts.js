/* Initial state */
const initialState = { count: 0 };

/* Action creators */
const add = () => ({ type: "ADD" });
const subtract = () => ({ type: "SUBTRACT" });
const reset = () => ({ type: "RESET" });